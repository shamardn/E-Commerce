<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.E_Commerce" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/color_primary</item>
        <!--        system ui configurations -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi,oreo">true</item>
    </style>

    <style name="Theme.E_Commerce" parent="Base.Theme.E_Commerce" />

    <style name="Theme.MainActivity" parent="Theme.E_Commerce">
        <item name="android:windowBackground">@drawable/launch_background</item>

        <!--        system ui configurations -->
        <item name="android:statusBarColor">@color/color_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/color_primary</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi,oreo">false</item>
    </style>
</resources>